## Contains SQL syntax and sample queries

# SQL Queries Part1:
SELECT, DISTINCT, WHERE, COUNT, LIMIT, ORDER BY, BETWEEN, IN, LIKE

# SQL Queries Part2:
Aggregare functions: AVG, SUM, MIN, MAX, COUNT
GROUP BY, HAVING, AS
INNER JOIN, OUTER JOIN, UNION

# SQL Queries Part3:
Timestamps and Extract
Mathematical functions
String functions
Subquery
Self Join

# SQL Queries Part4:
CREATE TABLE
INSERT, LIKE (copy)
UPDATE, DELETE, ALTER

# SQL Queries Part5:
DROP, CHECK, NOT NULL, UNIQUE
Views



